# BLOCS-ME · Дизайн-система Ember (лендинг v2)

Градиент — главный фирменный элемент. Всё остальное держится на тёплом графите
и слоновой кости; огонь появляется дозированно.

Реализация: базовый слой `css/style.css` (общий с v1) + оверрайд `v2/ember.css`,
который переопределяет токены и акцентные поверхности. Сборки нет — статика.

---

## 1. Цвет

### 1.1 Брендовый градиент

Три стопа одного «расплавленного» спектра: солнечное золото → танжерин → вермильон.

| Токен | Значение | Где применяется |
|---|---|---|
| `--ember-grad` | `linear-gradient(135deg, #F9B233 0%, #F87A1E 45%, #E93A0E 100%)` | главная кнопка, акцентные секции, CTA-панель, панель регистрации, карточка Insight |
| `--ember-grad-90` | `linear-gradient(90deg, #F9B233 0%, #F87A1E 45%, #E93A0E 100%)` | градиентный текст, полосы в дашборде |
| `--ember-grad-deep` | `linear-gradient(135deg, #F87A1E 0%, #E93A0E 55%, #B32806 100%)` | резерв: тёмные ховеры / иллюстрации |
| `--ember-umbra` | `#33140A` | **текст на градиенте** (не белый!) |
| `--ember-smolder` | `#6E1B08` | италик-акценты на градиенте |

Стопы по отдельности: `#F9B233` solar · `#F87A1E` tangerine · `#E93A0E` vermilion
· `#B32806` coal · `#6E1B08` smolder.

### 1.2 Нейтрали — тёплый графит

Серые с эспрессо-подтоном (ступени шкалы Ember gray), не холодные:
они не спорят с огнём, а согревают его.

| Токен | HEX | Ember-ступень | Роль |
|---|---|---|---|
| `--c-black` | `#0F0C09` | void | фон страницы, тёмные секции |
| `--c-coal` | `#292219` | overlay | границы, разделители, ховер-фон |
| `--c-line` | `#4E463C` | g700 | обводки кнопок, слабый текст на тёмном |
| `--c-muted` | `#8F8577` | g500 | подписи, eyebrow |
| `--c-soft` | `#ABA294` | g400 | подзаголовки секций |
| `--c-sand` | `#D0C9BF` | g300 | вторичный светлый текст |
| `--c-paper` | `#F7F1E8` | ink | основной текст на тёмном, светлые секции |

Дополнительные поверхности на светлом:

| HEX | Ember | Где |
|---|---|---|
| `#FFFFFF` | g0 | карточки слайдера проектов |
| `#F2EFEA` | g100 | карточки кейсов, бейджи прогресса/статуса |
| `#E5E0D8` | g200 | зона фото человека в кейсах |
| `#221C16` | g900 | текст на светлых карточках |

### 1.3 Акцент (плоский)

| Токен | HEX | Роль |
|---|---|---|
| `--c-accent` | `#F87A1E` | иконки, чипы, номера секций, пины, 3D-столбы |
| `--c-accent-hi` | `#F9B233` | подсветка, `::selection` на градиенте |

Используется там, где градиент технически невозможен или был бы шумом
(мелкие элементы, SVG-обводки, точки на карте).

### 1.4 Пропорции 60 / 30 / 8 / 2

Дорогой вид держится на дефиците огня:

- **60 %** — тёплый графит `#0F0C09` / `#292219` (фон, тёмные секции)
- **30 %** — светлые поверхности `#F7F1E8` / `#F2EFEA`
- **8 %** — брендовый градиент (главная кнопка + 2 акцентные секции)
- **2 %** — плоский танжерин в микроэлементах

---

## 2. Типографика

### 2.1 Гарнитуры

| Роль | Шрифт | Начертания |
|---|---|---|
| Display (`--font-display`) | **Archivo** variable | `wdth 62..125`, `wght 100..900` |
| Mono (`--font-mono`) | **JetBrains Mono** | 400 / 500 / 700 |
| Арабский | **Cairo** (заменяет обе роли) | 400–900 |
| Китайский | **Noto Sans SC** (после Archivo/JBM) | 400 / 500 / 700 / 900 |

База: `body` 17px / 1.6, `font-variation-settings: "wdth" 100`.

### 2.2 Шкала (значения v2 — на шаг мельче v1, чтобы не обрезались строки)

| Класс | Размер | Параметры |
|---|---|---|
| `.hero__title` | `clamp(30px, 3.9vw, 54px)` | 900, lh 1.02, ls −.025em, uppercase, wdth 108 |
| `.h2` | `clamp(32px, 4.4vw, 68px)` | 800, lh .98, ls −.025em, uppercase, wdth 112 |
| `.h2--sm` | `clamp(28px, 3.4vw, 50px)` | — |
| `.faq .h2--sm` | `clamp(26px, 2.7vw, 40px)` | — |
| `.cta__title` | `clamp(21px, 2.6vw, 40px)` | 900, lh 1.04, uppercase, wdth 112 |
| `.auth-title` | `clamp(23px, 2.3vw, 36px)` | 900, uppercase |
| `.stat__num` | `clamp(40px, 4.2vw, 68px)` | 900, wdth 112, tabular-nums |
| `.section-sub` | `clamp(17px, 1.5vw, 21px)` | lh 1.6, `--c-soft`, max-width 640px |
| `.hero__sub` | `clamp(16px, 1.35vw, 19px)` | lh 1.65, `--c-soft` |
| `.eyebrow` | mono 12px | ls .18em, uppercase, `--c-muted` |
| `.btn` | mono 13px / 600 | ls .1em, uppercase |
| trust-строка | mono 12px | uppercase, `--c-muted` |

### 2.3 Градиентный текст

Ключевые слова в заголовках (`24,000+`, `RFQ`, `revenue`) — италик, залитый градиентом:

```css
.hero__title .hl,
.accent-italic{
  background: var(--ember-grad-90);
  -webkit-background-clip: text;
  background-clip: text;
  color: transparent;
  /* хвосты италика выходят за бокс, а background-clip красит только внутри —
     расширяем область отрисовки, компенсируем отрицательными margin */
  padding: .1em .16em .12em .05em;
  margin: -.1em -.16em -.12em -.05em;
}
```

То же для строк-масок (`.split`), иначе хвосты Q/E обрезаются overflow:hidden:

```css
.hero__title .split, .h2 .split, .cta__title .split{
  padding-bottom: .09em; margin-bottom: -.09em;
  padding-inline-end: .1em; margin-inline-end: -.1em;
}
```

---

## 3. Сетка, отступы, радиусы

| Токен | Значение |
|---|---|
| `.container` | max-width **1440px**, `padding-inline: var(--pad-x)` |
| `--pad-x` | `clamp(20px, 4vw, 64px)` |
| `--nav-h` | `76px` |
| `--radius` | `18px` (карточки, панели) |
| крупные панели | `border-radius: clamp(20px, 2.4vw, 32px)` |
| пилюли / кнопки | `border-radius: 999px` |
| мелкие элементы | `calc(var(--radius) - 8px)` = 10px |
| отступ секции | `padding-block: clamp(90px, 12vw, 170px) clamp(52px, 6vw, 90px)` |

Брейкпоинт мобильной адаптации — **768px**, десктопная сетка приложения — **1024px**.

---

## 4. Компоненты

### 4.1 Кнопки

```css
.btn{ font-family: mono; font-size: 13px; font-weight: 600;
      letter-spacing: .1em; text-transform: uppercase; border-radius: 999px; }
.btn--sm{ padding: 11px 22px; font-size: 11.5px; }
.btn--lg{ padding: 18px 34px; }
```

| Вариант | Стиль |
|---|---|
| **solid** (главное действие) | фон `--ember-grad`, текст `--ember-umbra`; hover — `brightness(1.07) saturate(1.05)` + `box-shadow: 0 12px 40px -8px rgba(233,58,14,.45)` |
| **ghost** | `1px solid var(--c-line)`, текст `--c-paper`; hover — рамка `--c-paper` + фон 8 % paper |
| **dark** | фон `--c-black`, текст `--c-paper`; используется на светлых и градиентных секциях |

Правило: градиент — **только** на главном действии и в hero-зонах. Вторая кнопка всегда ghost или dark.

### 4.2 Переключатель языка (`<details class="lang-dd">`)

Визуально идентичен ghost-кнопке Log in: mono 11.5px, ls .1em, padding 11/20,
цвет `--c-paper`, рамка `--c-line`, радиус 999px. Выпадающий список — фон `#161614`,
радиус 14px, активный язык `--c-accent`. На градиентных поверхностях
(панель регистрации) — тёмный вариант: текст `--ember-umbra`, рамка 30 % чёрного.

### 4.3 Карточки

| Элемент | Стиль |
|---|---|
| Карточка проекта (`.scard`) | фон `#FFFFFF`, рамка `1px solid rgba(34,28,22,.08)`, **без тени**, радиус 18px |
| Бейдж прогресса/статуса | фон `#F2EFEA`, текст `#221C16`, радиус 9px, 12px/600 |
| Карточка кейса (`.case`) | фон `#F2EFEA`, зона фото `#E5E0D8`, текст `#111` |
| Тёмная карточка дашборда | фон `#1D1D1A` на `#161614` |

Тени в светлых блоках не используются — только тонкая обводка.

### 4.4 Акцентные поверхности

```css
.section--accent,          /* секция «для кого» */
.cta__panel,               /* финальный CTA */
.auth-brand,               /* левая панель регистрации */
.dash__insight{            /* карточка Insight в дашборде */
  background: var(--ember-grad);
  color: var(--ember-umbra);
}
.cta__title-last,
.auth-line--outline em{ color: var(--ember-smolder); }
```

### 4.5 Логотип

| Файл | Когда |
|---|---|
| `v2/logo-light.svg` | тёмный фон: шапка, футер, дашборд |
| `v2/logo-dark.svg` | светлый фон: топбар демо-интерфейса |
| `v2/logo-mono.svg` | градиентный фон: панель регистрации (чёрный бейдж, градиентный знак) |

Размеры: шапка / футер / регистрация **32px**, дашборд **19px**, топбар приложения **22px**.
Логотипы используются как есть, не перерисовываются.

---

## 5. Движение

| Параметр | Значение |
|---|---|
| `--ease-out` | `cubic-bezier(.22, 1, .36, 1)` — раскрытия, появления |
| `--ease-swift` | `cubic-bezier(.65, 0, .35, 1)` — переключения, шаги |
| Библиотека | GSAP 3.12.5 + ScrollTrigger (CDN) |
| Появление блоков | `.fade-up`: `opacity 0 → 1`, `translateY(42px) → 0` |
| Заголовки | построчная маска `.split` + подъём внутренней строки |
| Бегущая строка | `28s linear infinite`, пауза на hover |
| Зерно | фиксированный оверлей, `opacity .05`, `7s steps(10)` |
| Кнопки | `.magnetic` — притяжение к курсору; `.tilt-soft` — 3D-наклон панелей |

⚠️ `scroll-behavior: smooth` в CSS **запрещён** — ломает расчёт позиций ScrollTrigger.
Плавная прокрутка по якорям делается через JS `scrollIntoView`.

---

## 6. 3D-сцена героя (`v2/scene.js`, Three.js)

«Город блоков»: сетка 30×30 инстансов, дышит волнами, параллакс от курсора.

| Элемент | Цвет / параметр |
|---|---|
| Фон и туман | `0x0F0C09` |
| Ambient light | `0x8F8577`, 0.6 |
| Key light | `0xF7F1E8`, 1.5 |
| Rim light | `0x4A4A45`, 0.8 |
| Блуждающая подсветка | PointLight `0xF9B233`, интенсивность 70 (пульс ±16) |
| Тёмные блоки | `0x241E16` / `0x302820` / `0x3D342A` / `0x4E463C` |
| Огненные блоки (7 %) | `0xF87A1E` — один цвет, без градаций |
| Земля | `0x14100C` |

---

## 7. Мультиязычность

Три локали, единый код: `v2/` (EN) · `v2/ar/` (AR, RTL) · `v2/zh/` (ZH).

- **AR**: `<html lang="ar" dir="rtl">`, шрифт Cairo, `letter-spacing: 0 !important`,
  line-height 1.25 у крупных заголовков, синтетический италик отключён
  (акцент остаётся цветовым), интерфейсные моки зеркалятся, карта — географическая.
- **ZH**: `<html lang="zh-CN">`, Noto Sans SC, line-height 1.18, италик отключён.
- Тексты интерфейсных анимаций подменяются через `window.BLOCS_I18N`
  (`cardData`, `tourStops`, `requestSent`, `created` / `signed` / `connecting`) —
  `main.js` и `auth.js` не форкаются.
- Скейл-риги (`.demo-scale`, `.ppage-scale`) в RTL используют
  `transform-origin: top right`; слайдеры дрейфуют вправо (`xPercent: +50`).

---

## 8. Токены одним блоком

```css
:root{
  /* нейтрали — тёплый графит */
  --c-black:     #0F0C09;
  --c-coal:      #292219;
  --c-line:      #4E463C;
  --c-muted:     #8F8577;
  --c-soft:      #ABA294;
  --c-sand:      #D0C9BF;
  --c-paper:     #F7F1E8;

  /* бренд */
  --c-accent:    #F87A1E;
  --c-accent-hi: #F9B233;

  --ember-grad:      linear-gradient(135deg, #F9B233 0%, #F87A1E 45%, #E93A0E 100%);
  --ember-grad-90:   linear-gradient(90deg,  #F9B233 0%, #F87A1E 45%, #E93A0E 100%);
  --ember-grad-deep: linear-gradient(135deg, #F87A1E 0%, #E93A0E 55%, #B32806 100%);
  --ember-umbra:     #33140A;   /* текст на градиенте */
  --ember-smolder:   #6E1B08;   /* италик на градиенте */

  /* светлые поверхности */
  --surface-white:   #FFFFFF;
  --surface-paper:   #F2EFEA;
  --surface-sand:    #E5E0D8;
  --text-on-light:   #221C16;

  /* типографика */
  --font-display: "Archivo", system-ui, sans-serif;
  --font-mono:    "JetBrains Mono", ui-monospace, monospace;

  /* движение */
  --ease-out:   cubic-bezier(.22, 1, .36, 1);
  --ease-swift: cubic-bezier(.65, 0, .35, 1);

  /* геометрия */
  --nav-h:  76px;
  --pad-x:  clamp(20px, 4vw, 64px);
  --radius: 18px;
}
```

---

## 9. Правила, которые легко нарушить

1. Текст на градиенте — **умбра `#33140A`**, никогда не белый.
2. Градиент только на главном действии и hero-зонах; вторая кнопка — ghost или dark.
3. Серые — только из тёплой шкалы. Смешивать подтоны (синеватый + желтоватый)
   нельзя: выглядит как недоработка.
4. Тени на светлых карточках не использовать — только обводка `rgba(34,28,22,.08)`.
5. Италик на арабском и китайском отключается — акцент передаётся цветом.
6. Градиентному тексту и строкам-маскам нужен запас по боксу, иначе хвосты букв
   обрезаются.
7. Никакого `scroll-behavior: smooth` в CSS.
